% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gradients.R
\name{make_inv_and_grad_fixed}
\alias{make_inv_and_grad_fixed}
\title{Build Inverse, Gradient, and grad_y Closures for a Model}
\usage{
make_inv_and_grad_fixed(model, fixed_a = NULL)
}
\arguments{
\item{model}{Character. One of \code{"logistic4"}, \code{"logistic5"},
\code{"loglogistic4"}, \code{"loglogistic5"}, \code{"gompertz4"}.}

\item{fixed_a}{Numeric scalar or NULL. If non-NULL, \code{a} is treated as
a known constant and excluded from the parameter gradient.}
}
\value{
A list with closures: \code{inv(y, p)}, \code{grad(y, p)}, \code{grad_y(y, p)}.
Each accepts a response value \code{y} and named parameter vector \code{p}
(from \code{coef(fit)}).
}
\description{
Returns three closures (\code{inv}, \code{grad}, \code{grad_y}) that evaluate the inverse
function, parameter gradient, and response-derivative for a given model.
Used by error propagation via the delta method.
}

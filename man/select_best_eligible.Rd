% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eligibility.R
\name{select_best_eligible}
\alias{select_best_eligible}
\title{Select the Best Eligible Model}
\usage{
select_best_eligible(
  assessments,
  ranking_scores,
  criterion = "AIC+eligibility",
  higher_is_better = FALSE
)
}
\arguments{
\item{assessments}{Named list of \code{\link[=assess_model_eligibility]{assess_model_eligibility()}} outputs,
one entry per model name.}

\item{ranking_scores}{Named numeric vector.  For AIC: the AIC value (lower
is better, so provide as-is -- this function finds the minimum).  For LOO:
the elpd value (higher is better -- provide with \code{higher_is_better = TRUE}).}

\item{criterion}{Character.  Label stored in the returned selection object,
e.g. \code{"AIC+eligibility"} or \code{"LOO+eligibility"}.}

\item{higher_is_better}{Logical.  If TRUE, higher \code{ranking_scores} are
preferred (use for LOO elpd).  Default FALSE (use for AIC).}
}
\value{
A named list with:
\describe{
\item{best_model_name}{Character, or NA if no model converged.}
\item{criterion}{Character label.}
\item{fallback}{Logical. TRUE if no eligible model existed.}
\item{fallback_reason}{Character. Describes why fallback occurred.}
\item{assessments}{The full list of \code{\link[=assess_model_eligibility]{assess_model_eligibility()}} outputs.}
\item{eligible_models}{Character vector of eligible model names.}
}
}
\description{
Given per-model eligibility assessments and a named vector of ranking
scores, identifies the best eligible model.  If no model passes all gates,
falls back to the model with the widest dynamic range among converged models
and records \code{fallback = TRUE} in the selection.
}
\details{
This function is the shared selection layer used by both curveRfreq
(ranking by AIC) and curveRbayes (ranking by LOO elpd).  The \code{aic_selection}
or \code{loo_selection} objects computed by the framework-specific functions are
passed through unchanged and augmented with eligibility information.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy_extractors.R
\name{tidy_samples}
\alias{tidy_samples}
\alias{tidy_samples.calibration_result}
\alias{tidy_samples.calibration_result_multiplate}
\title{Tidy the per-sample predictions from a calibration result}
\usage{
tidy_samples(x, ...)

\method{tidy_samples}{calibration_result}(x, ...)

\method{tidy_samples}{calibration_result_multiplate}(x, ...)
}
\arguments{
\item{x}{A \code{calibration_result} or \code{calibration_result_multiplate}.}

\item{...}{Unused; for method extensibility.}
}
\value{
A data frame of the per-sample predictions. For multiplate input the
rows of every plate are row-bound with a \code{curve_id} column. Includes the
carried-through original sample columns plus \code{predicted_concentration},
\code{se_concentration}, \code{pcov}, \code{pcov_pass}, etc. Returns a zero-row frame if
no samples are present.
}
\description{
Extracts the \verb{$samples} table from a \code{calibration_result} or
\code{calibration_result_multiplate} into a single tidy data frame, attaching
\code{curve_id} for multiplate inputs. This is the canonical, supported way for
downstream packages (e.g. curveRweights) to read sample-level concentration
and precision; they must not reach into the object internals directly.
}
\seealso{
\code{\link[=tidy_grid]{tidy_grid()}}, \code{\link[=pcov_from_se]{pcov_from_se()}}
}

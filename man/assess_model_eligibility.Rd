% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eligibility.R
\name{assess_model_eligibility}
\alias{assess_model_eligibility}
\title{Assess Model Eligibility for Quantification}
\usage{
assess_model_eligibility(
  model_name,
  parameters,
  constraints = NULL,
  pcov_profile = NULL,
  grid_x = NULL,
  pcov_threshold = 20,
  bound_tol = 1e-04,
  max_rel_se = 5,
  min_dynamic_range_log10 = 0.5,
  vcov_matrix = NULL
)
}
\arguments{
\item{model_name}{Character.  Name of the model being assessed.}

\item{parameters}{Data frame.  Must contain \code{term} and either \code{estimate}
\itemize{
\item \code{std_error} (frequentist) or \code{mean} + \code{sd} (Bayesian).
}}

\item{constraints}{Named list with \verb{$lower} and \verb{$upper} named numeric
vectors for the free parameters, or NULL (Bayesian / fixed-a).}

\item{pcov_profile}{Numeric vector of pcov values (\%) on the prediction
grid, or NULL if the grid has not been computed for this model.}

\item{grid_x}{Numeric vector of log10_concentration values matching
\code{pcov_profile}, or NULL.}

\item{pcov_threshold}{Numeric.  The pcov (\%) threshold used to define the
LLOQ and ULOQ.  Default 20.}

\item{bound_tol}{Numeric.  A parameter estimate within this absolute
distance of a constraint bound is treated as "at bound".  Default 1e-4.}

\item{max_rel_se}{Numeric.  Maximum permitted relative SE
(\verb{SE / |estimate|}) for any parameter.  Default 5.0.}

\item{min_dynamic_range_log10}{Numeric.  Minimum required LLOQ-to-ULOQ
span in log10 concentration units.  Default 0.5 (~3-fold).}

\item{vcov_matrix}{Numeric matrix.  The parameter covariance matrix from
\code{vcov(fit)}, or NULL.  Used for the condition-number gate.}
}
\value{
A named list:
\describe{
\item{model_name}{Character.}
\item{eligible}{Logical.  TRUE if all applicable gates pass.}
\item{gates}{Data frame with columns \code{gate}, \code{passed}, \code{detail}.}
\item{dynamic_range_log10}{Numeric or NA.}
\item{lloq}{Numeric or NA (log10 scale).}
\item{uloq}{Numeric or NA (log10 scale).}
}
}
\description{
Applies a set of identifiability and precision gates to determine whether
a fitted model is reliable enough to be considered for selection as the
calibration model used for sample quantification.  The same gates are
applied in both the frequentist and Bayesian frameworks, with
framework-specific gates automatically skipped when the required inputs
are not available.
}

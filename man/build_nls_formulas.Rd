% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formulas.R
\name{build_nls_formulas}
\alias{build_nls_formulas}
\title{Build NLS Formulas for Candidate Models}
\usage{
build_nls_formulas(
  model_names,
  response_variable,
  independent_variable = "concentration",
  fixed_a = NULL,
  is_log_response = TRUE
)
}
\arguments{
\item{model_names}{Character vector. Which models to build formulas for.
Must be a subset of \code{\link[=available_models]{available_models()}}.}

\item{response_variable}{Character. Name of the response column in the
fitting data frame.}

\item{independent_variable}{Character. Name of the concentration column
in the fitting data frame. Default \code{"concentration"}.}

\item{fixed_a}{Numeric scalar or NULL. If non-NULL, the lower asymptote
is fixed at this value (on the \strong{raw} scale before any log transform).}

\item{is_log_response}{Logical. If TRUE and \code{fixed_a} is non-NULL,
log10-transform the fixed value before substitution.}
}
\value{
Named list of formula objects, one per model.
}
\description{
Constructs \code{stats::nls()}-compatible formula objects for the requested
models, using the curveRcore parameterisation (b > 0, always increasing).
}
\details{
When \code{fixed_a} is non-NULL and \code{is_log_response = TRUE}, the raw value
is validated and log10-transformed before substitution. When \code{fixed_a}
is non-NULL and \code{is_log_response = FALSE}, it is substituted directly.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pcov_gates.R
\name{classify_pcov_gate}
\alias{classify_pcov_gate}
\title{Classify pcov gate membership for a set of points on one curve}
\usage{
classify_pcov_gate(
  x_log10,
  pcov,
  pcov_threshold,
  lloq_log10 = NA_real_,
  uloq_log10 = NA_real_,
  inflect_x = NA_real_
)
}
\arguments{
\item{x_log10}{Numeric vector. log10-scale position: \verb{predicted_log10_ concentration} for test samples, \code{log10_concentration} for grid points.}

\item{pcov}{Numeric vector. Percent CV at each \code{x_log10}. Only used in the
fallback branch (when LLOQ and/or ULOQ are undefined).}

\item{pcov_threshold}{Numeric scalar. Only used in the fallback branch.}

\item{lloq_log10, uloq_log10}{Numeric scalars (log10 scale), or \code{NA} if
undefined for this curve/model.}

\item{inflect_x}{Numeric scalar (log10 scale). Only used in the fallback
branch, to decide which side of the curve a failing point falls on.
If \code{NA} and a point fails, that point's \code{pcov_gate_class} stays \code{NA}
(we know it fails, we just can't say which side) while \code{pcov_pass}
is still correctly \code{FALSE}.}
}
\value{
A data.frame with columns:
\describe{
\item{pcov_gate_class}{Factor: \code{belowLLOQ} / \code{inDynamicRange} /
\code{aboveULOQ} / \code{NA}. In the fallback branch, a failing point with no
\code{inflect_x} available is \code{NA} here (we know it fails, not which
side).}
\item{pcov_pass}{Logical, computed independently of
\code{pcov_gate_class} (NOT derived from it): \code{TRUE}/\code{FALSE} whenever the
point could be evaluated at all, \code{NA} only when \code{x_log10} (or, in
the fallback branch, \code{pcov}) itself is unusable. A failing point
with unknown side is \code{pcov_gate_class = NA} but \code{pcov_pass = FALSE}, never \code{NA}.}
}
Row count and order match the input vectors exactly.
}
\description{
Classify pcov gate membership for a set of points on one curve
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result_class.R
\name{new_calibration_result}
\alias{new_calibration_result}
\title{Construct a Calibration Result Object}
\usage{
new_calibration_result(
  meta,
  ensemble = list(),
  selection = list(),
  grid = data.frame(),
  samples = NULL,
  diagnostics = NULL
)
}
\arguments{
\item{meta}{Named list of metadata. Required fields:
\code{method} (\code{"frequentist"} or \code{"bayesian"}), \code{package}, \code{version},
\code{feature}, \code{antigen}, \code{plate}, \code{response_var}, \code{independent_var},
\code{is_log_response}, \code{is_log_independent}.}

\item{ensemble}{Named list of model fit results, one per model attempted.
Each element should be a list with \code{model_name}, \code{converged},
\code{parameters}, \code{fit_stats}, and optionally \code{raw_fit}.}

\item{selection}{Named list describing best-model selection:
\code{best_model_name}, \code{criterion}, \code{weights} (data.frame).}

\item{grid}{Data frame of grid predictions from the best model.}

\item{samples}{Data frame of test sample predictions from the best model,
or NULL if no samples were provided.}

\item{diagnostics}{Named list of diagnostic quantities (inflection point,
LODs, LOQs, etc.), or NULL.}
}
\value{
An object of class \code{calibration_result}.
}
\description{
Creates a validated \code{calibration_result} S3 object. This is the
canonical output of both \code{fit_calibration_freq()} (curveRfreq) and
\code{fit_calibration_bayes()} (curveRbayes).
}

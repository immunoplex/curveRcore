% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy_extractors.R
\name{tidy_draws}
\alias{tidy_draws}
\alias{tidy_draws.calibration_result}
\alias{tidy_draws.calibration_result_multiplate}
\title{Tidy the posterior / sampling draws from a calibration result}
\usage{
tidy_draws(x, ...)

\method{tidy_draws}{calibration_result}(x, ...)

\method{tidy_draws}{calibration_result_multiplate}(x, ...)
}
\arguments{
\item{x}{A \code{calibration_result} or \code{calibration_result_multiplate}.}

\item{...}{Unused; for method extensibility.}
}
\value{
Data frame \verb{curve_id, term, param_scope, sample_kind, n_draws, draws}
(\code{draws} is a list-column of numeric vectors).
}
\description{
Extracts per-parameter draw vectors (posterior draws for Bayesian fits;
asymptotic-MVN or bootstrap samples for frequentist) into a tidy frame with
one row per \verb{(curve_id, term)} and a list-column \code{draws} holding the vector,
plus \code{n_draws} and \code{sample_kind}. Feeds the \code{calib_draws} table (the worker
packs \code{draws} into a \code{float8[]} column). Present only when the fit was run
with \code{persist_draws = TRUE}; otherwise returns a zero-row frame.
}
\details{
Draw order is preserved and shared across terms within a fit, so consumers
may column-bind the vectors into a joint posterior (e.g. the JOB-3
correlation matrix).
}
\seealso{
\code{\link[=tidy_hyperparam]{tidy_hyperparam()}}
}
